<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Information</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar {
            justify-content: space-between;
        }
        .bg-custom {
            background-color: #333333;
        }
        .btn-group {
            display: flex;
            align-items: center;
        }
        .btn-group .btn {
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-light bg-custom">
        <a class="navbar-brand" href="/?username=<%= user.username %>" style="font-weight: bold; color: white;">Tic-Tac-Toe</a>
        <form class="form-inline ml-auto" action="/auth/profile" method="get">
            <button type="submit" class="btn btn-link"><%= user.username %></button>
        </form>
        <form class="form-inline" action="/auth/logout" method="post">
            <button type="submit" class="btn btn-danger">Logout</button>
        </form>
    </nav>
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center mb-4">Account Information</h1>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Username: <%= user.username %></h5>
                        <p class="card-text">Email: <%= user.email %></p>
                    </div>
                </div>
                <div class="text-center mt-3">
                    <a href="/auth/changePassword" class="btn btn-primary">Change Password</a>
                </div>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
